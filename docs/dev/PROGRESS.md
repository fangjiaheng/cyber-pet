# 开发进度

更新时间：2026-03-31

## 当前阶段结论

项目已经从“桌宠基础原型”推进到“主交互链路、任务系统、窗口锚点和本地持久化可用”的阶段。

当前阶段的关键结论：

- 主播放链路已经稳定在 `player.swf + loadlists`
- 功能区、右键菜单、喂食清洁滚动条、任务面板都已接入主交互链路
- 登录送礼 / 在线送礼已落地，并完成本地持久化
- 气泡提示已恢复到宠物头顶，显示时不会再把宠物整体向下顶动
- 主文档已从乱码状态恢复为可维护的中文版本

## 最新里程碑

### 里程碑 9：任务送礼接入

已完成：

- 新增 `src/shared/taskGift.ts`
- 建立登录送礼与在线送礼的状态模型
- 建立登录送礼 12 格周期与在线送礼 8 档在线时长配置
- 接入每天 `06:00` 的重置边界
- 接入 `petStore` 与 `storage` 的持久化链路
- 在主界面 `任务` 入口展示并领取奖励

### 里程碑 10：宠物与气泡锚点修正

已完成：

- `pet` 与 `bubble` 模式统一锚点
- `bubble` 窗口尺寸与 `pet` 保持一致
- 补充 `tests/petWindowLayout.test.ts`
- 验证窗口往返切换后宠物位置保持稳定

### 里程碑 11：文档修复

已完成：

- 重写主文档为中文 UTF-8
- 同步任务系统、测试状态与已知边界
- 清理原有文档中的大块乱码内容

## 本轮验证

建议每次改动后至少执行：

```bash
npm run type-check
npm run test
```

当前测试集：

- `tests/petWindowLayout.test.ts`
- `tests/taskGift.test.ts`

## 仍需推进

1. 继续清理源码中的历史乱码字符串
2. 评估任务奖励是否升级为真实道具/背包
3. 推进原创素材替换，逐步摆脱原版资源依赖

阶段关键词：稳定化、文档修复、任务系统落地
